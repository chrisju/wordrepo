======
抓文
查找特征:中文开头,可含英文 其他都不含(可扩展
c,tmpfs

步骤
0.整理文章 把汉字无关的都删掉 并合并到一个文章里

1.遍历文章
每偏移一个字
//保存新汉字(二分法排序链表)
保存新词或添加词频(二分法排序链表)

3.处理特征串
计算重复次数(从最大长度到长度2),保存重复出现的为词(可配置阈值

(可能需要筛掉只在一个数据源会出现的词

makerepo.py
tidy:根据核数分成n块
gen:fork出n个子进程并行生成词库
filter:过滤(只保留搜索长度-1长的词,等于搜索长度的词有待重新查询

