抓文
查找特征:中文开头,可含英文 其他都不含(可扩展
c,tmpfs

步骤
0.整理文章 把汉字无关的都删掉 并合并到一个文章里

1.遍历文章找首字 需重复汉字个数次数
找到一个如'好'则进行第二步,重复时会忽略好

2.遍历文章,保存'好'开始的特征串 最大数可设为10(可配置最大词长)

3.处理特征串
计算重复次数(从最大长度到长度2),保存重复出现的为词(可配置阈值

(可能需要筛掉只在一个数据源会出现的词
